# 3D Printing Platform - Telegram Bot

Telegram бот для приема заказов и уведомлений о статусе заказов.

## Возможности

- 📦 Оформление заказов через Telegram
- 📁 Загрузка 3D моделей
- 🎨 Выбор цветов и материалов
- 📊 Отслеживание статуса заказов
- 🔔 Уведомления об изменении статуса
- 💬 Интерактивное меню с кнопками

## Технологии

- **python-telegram-bot** - библиотека для работы с Telegram Bot API
- **aiohttp** - асинхронный HTTP клиент
- **asyncio** - асинхронное программирование

## Установка

### Требования

- Python 3.9+
- Telegram Bot Token (получить у @BotFather)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd telegram-bot
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Настройте переменные окружения:
```bash
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

### Получение Bot Token

1. Найдите @BotFather в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env` файл

## Запуск

### Режим разработки (polling)

```bash
python main.py
```

### Продакшн (webhook)

1. Настройте WEBHOOK_URL в `.env`:
```env
WEBHOOK_URL=https://your-domain.com/webhook
```

2. Запустите бота:
```bash
python main.py
```

### С помощью скрипта

```bash
chmod +x start.sh
./start.sh
```

## Конфигурация

### Обязательные переменные окружения

- `TELEGRAM_BOT_TOKEN` - токен вашего бота
- `API_BASE_URL` - URL бэкенд API

### Опциональные переменные

- `ADMIN_CHAT_IDS` - ID чатов администраторов (через запятую)
- `WEBHOOK_URL` - URL для webhook (если используется)
- `LOG_LEVEL` - уровень логирования (DEBUG, INFO, WARNING, ERROR)

Полный список переменных см. в `.env.example`

## Структура проекта

```
telegram-bot/
├── main.py                      # Основной файл бота
├── api_client.py               # Клиент для API
├── config.py                   # Конфигурация
├── session_manager.py          # Управление сессиями
├── order_handlers.py           # Обработчики заказов
├── subscription_manager.py     # Управление подписками
├── notification_service.py     # Сервис уведомлений
├── webhook_handler.py          # Обработчик webhook
├── health_check.py            # Health check endpoint
├── error_handler.py           # Обработка ошибок
├── logging_config.py          # Настройка логирования
├── requirements.txt           # Python зависимости
└── .env.example              # Пример конфигурации
```

## Команды бота

### Пользовательские команды

- `/start` - Начать работу с ботом
- `/help` - Справка по командам
- `/new_order` - Создать новый заказ
- `/my_orders` - Мои заказы
- `/track_order` - Отследить заказ
- `/subscribe` - Подписаться на уведомления
- `/cancel` - Отменить текущую операцию

### Админ команды

- `/stats` - Статистика бота
- `/broadcast` - Рассылка сообщений

## Мониторинг

### Health Check

Бот предоставляет health check endpoint:

```bash
curl http://localhost:8080/health
```

### Метрики

Метрики доступны по адресу:

```bash
curl http://localhost:8080/metrics
```

### Логи

Логи сохраняются в файл `bot.log` с автоматической ротацией.

## Тестирование

```bash
pytest
```

Запуск конкретных тестов:

```bash
pytest test_order_process.py
pytest test_session_manager.py
```

## Деплой

### Docker

```bash
docker build -t 3d-printing-telegram-bot .
docker run -d --env-file .env 3d-printing-telegram-bot
```

### Systemd Service

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=3D Printing Telegram Bot
After=network.target

[Service]
Type=simple
User=botuser
WorkingDirectory=/path/to/telegram-bot
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl enable telegram-bot
sudo systemctl start telegram-bot
sudo systemctl status telegram-bot
```

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл с реальным токеном
- Храните токен бота в безопасности
- Используйте webhook с HTTPS в продакшене
- Ограничьте доступ к админ командам
- Регулярно обновляйте зависимости

## Troubleshooting

### Бот не отвечает

1. Проверьте токен бота
2. Убедитесь, что бот запущен
3. Проверьте логи: `tail -f bot.log`

### Ошибки подключения к API

1. Проверьте `API_BASE_URL` в `.env`
2. Убедитесь, что бэкенд запущен и доступен
3. Проверьте сетевое подключение

### Webhook не работает

1. Убедитесь, что используется HTTPS
2. Проверьте `WEBHOOK_URL` в `.env`
3. Проверьте, что порт открыт

## Документация

Подробная документация доступна в файлах:
- `ORDER_PROCESS_DOCUMENTATION.md` - процесс оформления заказа
- `DEPLOYMENT.md` - инструкции по деплою

## Лицензия

Proprietary
