/app/logs/*.log {
    daily
    rotate 7
    compress
    delaycompress
    missingok
    notifempty
    create 644 botuser botuser
    postrotate
        # Send SIGUSR1 to the bot process to reopen log files
        pkill -USR1 -f "python.*main.py" || true
    endscript
}